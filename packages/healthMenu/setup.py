from setuptools import setup

setup(
      name='healthMenu',
      version='1.0',
      description='Module for menus and meals for BeHealthy',
      author='Me',
      author_email='linked2u@gmail.com',
      packages=['healthMenu'],
      install_requires=['wheel']
      )
